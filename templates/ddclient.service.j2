[Unit]
Description=Dynamic DNS Update Client
After=network.target

[Service]
Type=forking
PIDFile={{ ddclient_pidfile }}
ExecStart={{ ddclient_executable_path }}

[Install]
WantedBy=multi-user.target
